# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# Vercel / Node
.vercel/
node_modules/
*.log
npm-debug.log*

# Build intermediates
*.zip
*.bak
*.tmp
*.cache
~$*

# === SECURITY: never commit RBZ packages ===
# RBZ is just a ZIP — pushing them publicly exposes the full Ruby/JS source.
# Distribute via Extension Warehouse only.
*.rbz
releases/

# Local env
.env
.env.*
!.env.example
